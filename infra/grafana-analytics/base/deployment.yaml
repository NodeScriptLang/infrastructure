apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-analytics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-analytics
  template:
    metadata:
      labels:
        app: grafana-analytics
    spec:
      nodeSelector:
        workload/monitoring: "true"
      securityContext:
        fsGroup: 472
        supplementalGroups: [0]
      volumes:
        - name: config
          configMap:
            name: grafana-analytics-config
      containers:
        - name: grafana-analytics
          image: grafana-analytics
          ports:
          - name: http
            containerPort: 3000
          volumeMounts:
            - name: config
              mountPath: /etc/grafana
          envFrom:
            - secretRef:
                name: grafana-analytics-secrets
          resources:
            requests:
              cpu: 100m
              memory: 500Mi
            limits:
              memory: 1Gi

kind: Service
apiVersion: v1
metadata:
  name: registry-service
spec:
  type: ClusterIP
  selector:
    app: registry-service
  ports:
    - name: http
      port: 8080
      targetPort: http
---
kind: Service
apiVersion: v1
metadata:
  name: registry-service-aux
  labels:
    monitor: "true"
spec:
  type: ClusterIP
  selector:
    app: registry-service
  ports:
    - name: aux
      port: 8081
      targetPort: aux

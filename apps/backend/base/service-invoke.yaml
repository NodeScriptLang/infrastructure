kind: Service
apiVersion: v1
metadata:
  name: backend-invoke
  annotations:
    metricsPort: "8080"
spec:
  type: ClusterIP
  selector:
    app: backend-invoke
  ports:
    - name: http
      port: 8080
      targetPort: 8080
